@using DataValidation.Models
@model dynamic


<h2>Customers</h2>
<ul>
    @foreach(DataAnnotation model in Model)
    {
        <li>@Html.ActionLink(model.Email, "Show", new {id=model.Id})</li>
    }
</ul>

@Html.ActionLink("Create", "New", "DataAnnotations")